import EmptyState from '../components/EmptyState.jsx';

export default function MarketPage() {
  return (
    <EmptyState
      icon="🛒"
      title="Market"
      text="The market will show quest decks and memory tickets once the server is connected."
    />
  );
}
